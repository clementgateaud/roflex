<div class="container my-4">
  <div class="row">
    <div class="col-sm-4">
      <%= cl_image_tag @offer.photo.key, class:"w-100 mb-3" %>
    </div>
    <div class="col-sm-8">
    <div class="info-container">
      <h1><%= @offer.name %></h1>
      <p><%= @offer.description %></p>
      <h4>Price per day</h4>
      <p><strong><%= @offer.price %> €</strong></p>
    </div>
    <div class="action-container">
      <%= link_to 'Rent it now !', new_rental_path(@offer.id), class:"btn-rent" %>
    </div>
    </div>
  </div>
</div>
