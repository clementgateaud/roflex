<div class="container mt-5">
  <div class="row">
    <div class="col-sm-6">
      <%= cl_image_tag @offer.photo.key, class:"img-show w-100 mb-3" %>
    </div>
    <div class="col-sm-6">
    <div class="info-container">
      <h1 class="title"><%= @offer.name %></h1>
      <p><%= @offer.description %></p>
      <h4>Price per day : </h4>
      <p><strong><%= @offer.price %> €</strong></p>
    </div>
    <div class="action-container">
      <%= link_to 'Rent it now', new_rental_path(@offer.id), class:"btn btn-primary" %>
    </div>
    </div>
  </div>
</div>
