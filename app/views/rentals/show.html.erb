<div class="container mt-5 offershow">
  <div class="row">
    <div class="col-sm-6">
      <%= cl_image_tag @rental.offer.photo.key, class:"img-show w-100 mb-3" %>
    </div>
    <div class="col-sm-6">
     <div class="info-container">
      <h2><%= @rental.offer.name %></h2>
      <p><%= @rental.offer.description %></p>
      <h4><i class="fas fa-euro-sign"></i> Total price</h4>
      <p><strong><%= '%.2f' % @rental.total_amount %> €</strong></p>
      <h4>Rental dates</h4>
      <p><i class="fas fa-tag"></i> From <strong><%= @rental.start_time.strftime("%d/%m/%Y") %></strong>  to <strong><%= @rental.end_time.strftime("%d/%m/%Y") %></strong> </p>
    </div>
    </div>
  </div>
</div>
